<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Menu" parent="AppTheme">
        <item name="android:itemBackground">@color/colorPrimary</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>

    <style name="Browser" parent="Theme.AppCompat.DialogWhenLarge">
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:fontFamily">@font/raleway_semibold</item>
    </style>

    <style name="popup_window_animation">
        <item name="android:windowEnterAnimation">@anim/animation_grow_size</item>
        <item name="android:windowExitAnimation">@anim/animation_shrink_size</item>
    </style>
</resources>
